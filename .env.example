# Flask / Google Tasks runtime config
PORT=5000
INGEST_TOKEN=changeme
TASKLIST_ID=
TASKLIST_TITLE=
# Optional: substring used to detect the USB barcode scanner (macOS only)
SCANNER_IDENTIFIER=
# Optional: poll frequency (ms) for scanner status badge on dashboard
SCANNER_STATUS_REFRESH_MS=5000
