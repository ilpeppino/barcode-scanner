# Core runtime config
PORT=5000
TASKLIST_TITLE=Shopping
CURRENT_APP_VERSION=v1.3.0

# Flask session + OAuth Web Client
FLASK_SECRET=replace-me-with-a-strong-secret
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional override; otherwise derived from the current request URL.
GOOGLE_AUTH_REDIRECT_URI=https://scanner.example.com/auth/callback

# Optional deploy alerts (ntfy/Slack/etc.)
DEPLOY_ALERT_WEBHOOK=
DEPLOY_ALERT_HOSTNAME=home-nas
